{
  "buildCommand": "cd iro-admin && npm install && npm run build",
  "outputDirectory": "iro-admin/dist",
  "installCommand": "cd iro-admin && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
